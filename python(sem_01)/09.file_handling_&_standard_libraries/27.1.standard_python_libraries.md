# Standard Python Libraries

## Commonly Used Standard Libraries

### math
```python
import math
math.sqrt(16)       # 4.0
math.pi             # 3.14159...
math.sin(math.pi/2) # 1.0
```

### random
```python
import random
random.randint(1, 100)  # Random integer
random.choice([1,2,3])  # Random choice
random.shuffle(list)    # Shuffle list
```

### datetime
```python
from datetime import datetime, timedelta
now = datetime.now()
print(now.year, now.month, now.day)

tomorrow = now + timedelta(days=1)
```

### json
```python
import json
# Convert to JSON string
json_str = json.dumps({"name": "Alice", "age": 30})

# Convert from JSON
data = json.loads(json_str)
```

### os
```python
import os
os.getcwd()         # Current directory
os.listdir()        # List files
os.path.join(a, b)  # Join paths
```

### sys
```python
import sys
sys.version         # Python version
sys.exit()          # Exit program
sys.argv            # Command line arguments
```

For comprehensive library documentation, see detailed sections.
