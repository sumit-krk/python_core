# Function Parameters and Arguments

## Parameters vs Arguments
- **Parameters:** Variables in function definition
- **Arguments:** Values passed to function

### Types of Arguments

## Positional Arguments
```python
def greet(name, age):
    print(f"{name} is {age}")

greet("Alice", 30)      # Positional
```

## Keyword Arguments
```python
greet(age=30, name="Bob")  # Can reorder
greet(name="Charlie", age=25)
```

## Default Parameters
```python
def greet(name, greeting="Hello"):
    return f"{greeting}, {name}"

greet("Alice")          # Uses default
greet("Bob", "Hi")      # Override default
```

## Variable Length Arguments
```python
# *args - variable positional
def sum_all(*args):
    return sum(args)

sum_all(1, 2, 3, 4)

# **kwargs - variable keyword
def print_info(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

print_info(name="Alice", age=30)
```

See dedicated sections for comprehensive function documentation.
