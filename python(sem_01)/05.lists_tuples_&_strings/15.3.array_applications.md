# List Applications in Python

## Common List Applications

### 1. Data Storage
```python
# Store student marks
marks = [85, 90, 78, 92, 88]
average = sum(marks) / len(marks)
```

### 2. Stack Implementation
```python
stack = []
stack.append(1)         # Push
stack.pop()             # Pop
```

### 3. Queue Implementation
```python
queue = []
queue.append(1)         # Enqueue
queue.pop(0)            # Dequeue
```

### 4. Sorting and Searching
```python
numbers = [3, 1, 4, 1, 5, 9]
numbers.sort()          # Sort ascending
index = numbers.index(4)  # Find index
```

### 5. List Comprehension
```python
# Create new list from existing
squares = [x**2 for x in range(5)]  # [0, 1, 4, 9, 16]
```

For detailed examples and practical implementations, see dedicated sections.
